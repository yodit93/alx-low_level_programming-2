#include "main.h"
/**
 * _islower - function for checking the lower case
 * @c: character to be checked
 * Return: 1 if it is lower case or 0 if other
 */
int _islower(int c)
{


if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}

}
