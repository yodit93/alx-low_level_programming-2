#include "main.h"
/**
 * _abs - function for returning the absolute value of a given number
 * @n: number whose absolute value will be calculated
 * Return: absolute value of a number
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (n * (-1));
	}
	else
	{
	return (n);
	}
}

